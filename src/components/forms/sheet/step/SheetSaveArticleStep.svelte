<script lang="ts">
	import StepButton from '@/components/layout/StepButton.svelte';
	import StepLayout from './SheetStepLayout.svelte';
	import SheetSaveArticle from './SheetSaveArticle.svelte';

	let {
		form = $bindable(),
		onNext,
		onPre
	}: { form: SheetFormType; onNext: () => void; onPre: () => void } = $props();
</script>

<StepLayout
	currentStep="保存文章内容吗？"
	description="电子表格只能保存文章的标题、链接等元数据。如需将文章内容也一并保存到飞书文档，请在下方选择你需要关联的飞书文档保存配置。"
>
	<SheetSaveArticle bind:form />
	{#snippet footer()}
		<div class=" flex flex-row gap-4">
			<StepButton isDisable={false} onclick={onPre} description="上一步" />
			<StepButton isDisable={false} onclick={onNext} description="下一步" />
		</div>
	{/snippet}
</StepLayout>
